# Vault

Put exports here. Nothing in `vault/` is committed by default.

Recommended layout:

- `vault/chatgpt/` (ChatGPT export files)
- `vault/perplexity/` (Perplexity thread exports; prefer Markdown)

The indexer will scan these directories.
